<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Feedback | <%= title %></title>
    <style>
        @import url('https://fonts.googleapis.com/css2?family=Raleway:wght@400;700&display=swap');
        body {
            max-width: 1200px;
            margin: 20px auto;
            padding: 0 20px;
            font-family: "Raleway";
        }
        p, h1, h2, h3, a, ul {
            margin: 0;
            padding: 0;
            text-decoration: none;
            color: #222;
        }
        nav {
            display: flex;
            justify-content: space-between;
            margin-bottom: 60px;
            padding-bottom: 10px;
            border-bottom: 1px solid #ddd;
            text-transform: uppercase;
        }
        nav ul {
            display: flex;
            justify-content: space-between;
            align-items: flex-end;
        }
        nav li {
            list-style-type: none;
            margin-left: 20px;
        }
        nav h1 {
            font-size: 3em;
        }
        nav p, nav a {
            color: #777;
            font-weight: 300;
        }
        h2 {
            margin-bottom: 40px;
        }
        h3 {
            text-transform: capitalize;
            margin-bottom: 8px;
        }
        .content {
            margin-left: 20px;
        }
        .feedback a {
            display: block;
            margin: 40px 0;
            padding-left: 30px;
            border-left: 6px solid crimson;
        }
        .feedback a:hover h3 {
            color: crimson;
        }
        .details {
            position: relative;
        }
        .delete {
            position: absolute;
            top: 0;
            right: 0;
            border-radius: 50%;
            padding: 8px;
        }
        .delete:hover {
            cursor: pointer;
            box-shadow: 1px 2px 3px rgba(0, 0, 0, 0.2);
        }
        .create-blog form {
            max-width: 400px;
            margin: 0 auto;
        }
        .create-blog input,
        .create-blog textarea {
            display: block;
            width: 100%;
            margin: 10px 0;
            padding: 8px;
        }
        .create-blog label {
            display: block;
            margin-top: 24px;
        }
        textarea {
            height: 120px;
        }
        .create-blog button {
            margin-top: 20px;
            background: crimson;
            color: white;
            padding: 6px;
            border: 0;
            font-size: 1.2em;
            cursor: pointer;
        }
        .site-title {
            margin-top: 30px;
        }
    </style>
</head>
<body>
    <nav>
        <div class="site-title">
            <p>Please let us know how your night went</p>
        </div>
        <ul>
            <li><a href="/index">All Feedback</a></li>
            <li><a href="/create">New Feedback</a></li>
        </ul>
    </nav>

    <div class="feedback content">
        <h2>All Feedback</h2>

        <% if (feedback.length > 0) { %>
            <% feedback.forEach(feedback => { %>
                <a class="single" href="/feedback/ <%= feedback._id %>">
                    <h3 class="title"><%= feedback.title %></h3>
                    <p class="snippet"><%= feedback.snippet %></p>
                </a>
            <% }) %>
        <% } else { %>
            <p>There is no feedback to display.</p>
        <% } %>
    </div>
</body>
</html>